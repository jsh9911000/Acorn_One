<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<select id="isExist" parameterType="java.lang.String" resultType="string">
		SELECT id 
		FROM users
		WHERE id=#{id}
	</select>
	
	<insert id="insert" parameterType="memberDto">
		INSERT INTO member
		(id, pwd, name, age, gender, regdate)
		VALUES(#{id }, #{pwd }, #{name }, #{age }, #{gender }, SYSDATE)
	</insert>
	
	<select id="getDate" parameterType="string" resultType="memberDto">
		select id, pwd, name, age, gender, regdate
		from member
		where id=#{id }
	</select>
	
	<update id="pwdUpdate" parameterType="memberDto">
		update member
		set pwd=#{newPwd }
		where id=#{id }
	</update>
	
	<update id="update" parameterType="memberDto">
		update member
		set gender=#{gender }, age=#{age }
		where id=#{id }
	</update>
	
	<delete id="delete" parameterType="string">
		delete from member
		where id=#{id}
	</delete>
</mapper>